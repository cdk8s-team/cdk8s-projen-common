# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: Dependabot-Security-Notifications
on:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  dependabot_security_notification:
    name: dependabot-security-notification
    runs-on: ubuntu-latest
    permissions:
      security-events: read
      issues: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install and Build
        run: yarn install --frozen-lockfile && yarn build && yarn test && cd scripts
      - name: Run Script
        uses: ./scripts/createDependabotIssue.ts
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          REPO_ROOT: ${{ github.workspace }}
          REPO_NAME: ${{ github.repository }}
          OWNER_NAME: ${{ github.repository_owner }}
